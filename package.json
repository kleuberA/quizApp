{
  "name": "quiz",
  "version": "1.0.0",
  "description": "app quiz",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "lite-server --baseDir=dist",
    "start": "concurrently \"npm run watch\" \"npm run server\"",
    "compile": "tsc",
    "watch": "tsc -w",
    "iniciar": "node servidor-api/server.js"
  },
  "author": "Kleuber Silva",
  "license": "ISC",
  "dependencies": {
    "@types/jquery": "^3.5.9",
    "concurrently": "^6.4.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
